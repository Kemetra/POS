# 023 production integration execution ledger

Baseline: `feat/023-pos-ui-clean-room-sale-proof` at `ca9486c`, with the approved proof uncommitted. No cutover, commit, push, or PR.

- A1 cart lines: complete. RED: neutral controller test could not resolve the new module; subsequent mutation tests failed on the missing options/methods. GREEN: 5 controller tests; 66 focused cart/checkout tests; full suite 490 files, 5,698 passed, 3 skipped; typecheck and changed-file ESLint passed. `CartPane` now consumes the one neutral line controller; legacy route/UI remain mounted.
- Ruling: keep the `CartPane` type re-export while moving the type owner to the neutral controller, because existing tests import the public legacy type path. Cost if wrong: a temporary compatibility export remains until legacy retirement.
- A2 cart handoff: complete. RED: controller tests failed for missing handoff/discount/void methods. GREEN: 8 controller tests; 50 focused cart/checkout tests; full suite 490 files, 5,701 passed, 3 skipped; typecheck and changed-file ESLint passed. `CartPane` now delegates frozen-envelope, totals, discount-removal, void and checkout-store timing to the same neutral controller. Checkout route is unchanged.
- B catalogue orchestration: cart creation, typed-search and scan response mapping now live in `useSaleCatalogueController`; confirm-first add lives in `useConfirmSaleAdd`; freshness/refresh timing lives in `useCatalogueFreshness`. Legacy presentation consumes all three neutral hooks. RED: each new hook test initially failed on the missing module. GREEN: 39 focused catalogue/keyboard tests and 16 focused freshness tests; full suite before freshness extraction: 492 files, 5,705 passed, 3 skipped. Post-extraction typecheck and changed-file ESLint passed. Full suite after freshness extraction remains to run.
- B closeout (2026-09-24): full suite after freshness extraction ran green — 494 files, 5,709 passed, 3 skipped.
- C/D/E recording gap: the live adapter (`v5/sale/LiveSaleWorkspace.tsx`, `LiveProductRail.tsx`, `LiveSaleCart.tsx`, `live-sale.css`), its first three adapter tests, and the DEV-only `/app/sale-v5` route were already in the tree but unrecorded. Those were not written test-first. Everything below was.
- C/D gaps closed (TDD): RED — 5 of 14 adapter tests failed: confirm focus stayed in the scan field so Escape could not dismiss; note/void dialogs neither focused a control nor returned focus; an empty metadata span rendered for results without pack/barcode/SKU; secondary buttons were 40px. The parity tests (generic add/handoff refusal, versioned increment/decrement, stale-version no-op, decrement-at-one remove, cashier-vs-manager post-handoff void) passed immediately against the A/B hooks. GREEN: new `v5/sale/SaleDialog.tsx` (focus a chosen control on open, Escape dismiss, restore focus to opener); confirm opts out of restore because `onResolved` returns focus to search, mirroring legacy `ProductConfirmPanel`/`CatalogueSalePane`; conditional metadata span; 44px button floor asserted by a static CSS test. 16 v5 tests and the dialog-Escape guard pass.
- E routing (TDD): new `tests/integration/renderer/v5/sale-v5-route.test.tsx` through the real `AppRouter` — signed-out `/app/sale-v5` redirects to `/sign-in` with no `cart.create`; signed-in scan→confirm→handoff→Continue reaches the unchanged `/app/checkout` `PaymentSurface` with the frozen envelope. RED for the packaged-build criterion: a renderer build of the static-import wiring shipped every `v5-live-*`/`v5-sale-*` rule in production CSS (JS branch was dropped). GREEN: `router.tsx` now loads `LiveSaleWorkspace` via `lazy()` inside the DEV branch, with the inline DEV check kept in the route table so the route literal also folds away. Renderer build + grep for `sale-v5|v5-live|v5-sale|LiveSaleWorkspace|sale-proof` → no matches.
- C/D criteria follow-up (TDD): 8 more adapter tests. Seven passed first run, confirming parity: sign-out unmounts local lines and the next sign-in starts empty; `payments: false` keeps Continue disabled and never mounts the envelope; refused note save keeps the dialog open with generic copy; a quantity-one line carrying a note decrements rather than removes (legacy `QuantityStepper` rule); void calls the bridge and shows the cancelled state without line controls; refused void keeps the dialog open with no reason shown (matches legacy `CartPane`). RED on legacy `LineNotePopover` rules missing from v5 — 200-char cap, Save disabled when unchanged, Clear (saves `null`, disabled with no note). GREEN after adding them to `LiveSaleCart`.
- Gates (final): typecheck, changed-file ESLint (0 warnings), Prettier, `git diff --check`, renderer build + bundle grep clean, full suite 495 files, 5,730 passed, 3 skipped. Repository-wide lint still blocked by the unrelated ignored `.impeccable/hook.cache.json` (unchanged). `npm run typecheck` does not cover `tests/**`; the new integration test is covered by type-aware ESLint.
- Open, recorded not fixed: (1) plan risk #5 — the neutral handoff has a refusal rollback but no `catch` for a rejected transport promise; separate focused fix. (2) Plan risk #4 — overlapping typed searches have no response identity; stale-result race unchanged. (3) Legacy-parity observation: while confirm is open, focus sits on Add, so a second wedge scan's Enter terminator activates Add for the first product — identical in legacy; owner decision before any change. (4) No cart-read contract: entering `/app/sale-v5` with an `activeCart` created on `/app/cart` shows no lines; use a fresh cart per preview run.
- Stop point (superseded 2026-09-24): F completed below; G (cutover) needs the owner.
- F real Electron review (2026-09-24): done — see [slice-f-visual-review.md](./slice-f-visual-review.md). Fresh cart per run (reload boots to dashboard, v5 creates its own cart); fresh temporary profile; unpackaged-only dev fixtures; dead API base. Scenarios A–F pass at 1024 and 1280. v5-only presentation fixes: dialog button hierarchy (blocking drift), 44px padded secondary buttons, quiet line actions, single-line money, single search focus ring, scan placeholder, and display-only controlled/Rx badges (TDD: RED on missing badges → GREEN). Gates: typecheck, changed-file lint, Prettier, diff check, bundle grep clean, full suite 495 files, 5,731 passed, 3 skipped. Owner decision before G: app-shell chrome around v5 (finding 8). Cart-read bridge remains a cutover blocker.
- CI parity (2026-09-24): CI coverage thresholds initially failed for `LiveSaleCart`, `LiveProductRail` and legacy `CatalogueFreshness` (a guard branch left uncovered by the B extraction). Added `v5/__tests__/live-sale-states.test.tsx` and a freshness test. RED exposed a real v5 crash — an unparseable timestamp threw `RangeError` — fixed with the same guard legacy uses. `npm test -- --coverage`: 496 files, 5,745 passed, 3 skipped, thresholds met. Committed and PR opened at the owner's request; owner visual reconfirmation of the functional captures is requested in the PR.
- V5 active cart read (2026-09-24, `feat/v5-active-cart-read`, uncommitted): **supersedes this plan's "do not add an IPC method" note by explicit owner authorisation.** Adds one read-only `cart.snapshot` (`cart:snapshot`), which is session-gated first and ownership/tenant-gated. It uses the existing `getActiveLines` / `getDiscountPlaceholdersForCart`, performs no writes, and returns a display-safe projection plus the persisted envelope only when the cart is frozen. `useSaleCartController({ hydrateActiveCart: true })` rebuilds the projection only for a cart that already exists when the controller mounts; a failed read shows a generic retryable state and never creates a replacement cart. Legacy `CartPane` does not opt in, so its behaviour is unchanged. TDD: 33 RED → GREEN; full suite 502 files, 5,804 passed, 3 skipped, coverage met. Real Electron in one renderer session: legacy cart `e811b810…` (4 lines, quantities 2/1/2/1, one note, 172.00 EGP) reopened in `#/v5/sale` identically; carts 8 → 8, outbox 8 → 8 on mount. A V5 increment on the hydrated version was accepted (v2 → v3, outbox 9, same cart). §A4 record: `security-review/s-cart-snapshot-review.md` (reviewer sign-off owed). Remaining: the reverse direction (V5 → legacy remount) still loses lines, and renderer-restart discovery is out of scope. Evidence: `screenshots/continuity-{1-legacy-cart,2-v5-hydrated,3-v5-after-increment}.png`.
- V5 polish pass (2026-09-25, uncommitted, on `main` @ `959c68c`): presentation-only polish of `#/v5/sale` — see [polish-pass-review.md](./polish-pass-review.md). 18 new tests in `v5/__tests__/live-sale-polish.test.tsx`: 16 RED → GREEN, 1 passed on first run (no label while editing), 1 written test-after (frozen-region, added with its fix after the Electron axe finding). Gates: typecheck, V5 ESLint/Prettier, diff check, renderer build + bundle grep clean, impeccable detector clean, `npm test -- --coverage` 505 files, 5,841 passed, 3 skipped, thresholds met. Repository lint still fails only on ignored `.impeccable/hook.cache.json`. Electron evidence `screenshots/v5-polish-{1024,1280}-*.png`. Recorded, not fixed: post-handoff manager void is offered by the shared `canVoidCart`/`voidCart` path but main's `cart.void` refuses `frozen`, and main's `cancelPostHandoff` is not exposed through preload/bridge — so legacy `/app/cart` has the same defect today (pre-existing correctness defect, not V5-only); axe `nested-interactive` on result rows (pre-existing).
- Post-handoff cancel fix (2026-09-25, `fix/v5-post-handoff-void`, uncommitted): the new `cart:cancelPostHandoff` channel (preload + IPC validator, which drops `attribution_operator_id`) exposes the existing `CartBridge.cancelPostHandoff`. The shared `useSaleCartController.voidCart` routes `frozen_handed_off` carts to it using the frozen envelope's `handoff_action_id`, and fails closed on no envelope, a mismatched or empty handoff id, a missing bridge method, or a known started/settled payment. Owner-approved main guard: `bindCartPaymentGuard` makes `cancelPostHandoff` refuse `closed` for a started or settled payment. On a confirmed cancel the controller drops the cart's envelope, locally and in the payment store, because `payments.start` does not re-read cart state and legacy `CartPane` would otherwise keep a cancelled cart's envelope mounted for checkout. See the report for tests.
- **Owner visual approval (2026-09-25):** the owner reviewed the polish-pass captures (`screenshots/v5-polish-{1024,1280}-*.png`) and approved them in session ("1 ok approved"). This satisfies the `spec.md` gate, "Yes — this is the visual direction", for the V5 Sale + V5Frame composition. Slice G still waits on §A4 reviewer sign-off for `cart.snapshot` and `cart:cancelPostHandoff`.
- **§A4 review closure (2026-09-26, `fix/cart-payment-authority`):** an independent adversarial review of `cart.snapshot` and `cart:cancelPostHandoff` returned REVISE on both. Every finding is addressed, with tests first:
  - Main now refuses to pay a cancelled, out-of-scope, mismatched or already-paid cart (`payments.start` checks `bindCartPaymentEligibility`), and finalize refuses a cancelled cart.
  - Snapshot reports a settled cart as `paid` with no envelope; V5 honours it.
  - Manager attribution is removed from every renderer envelope.
  - cancelPostHandoff verifies the handoff action, binds replays to the cart, fails closed without the payment-status source, blocks on `force_failed`, and runs check-and-cancel in one transaction.
  - IPC ids are bounded.
  - Both security records were rewritten to match the code; reviewer sign-off is owed.
- **Owner decisions (2026-09-26):** (1) an independent re-review of `cart.snapshot` / `cart:cancelPostHandoff`, then owner sign-off; (2) G0 V5Frame parity before G, and at G wrap **both** `/app/cart` and `/app/checkout` in V5Frame so the chrome never changes mid-sale; (3) #466: the cart is created by the first confirmed add. The re-review returned APPROVE-WITH-NOTES; its two MEDIUM money findings are fixed in #472 and the records updated. #466 is #471.
- **G0 V5Frame parity (2026-09-26, `feat/023-g0-v5frame-parity`):** a closer read showed `AppShell` does less than first listed: no screen calls `operator.signOut()` (the bridge method exists; neither shell has a sign-out control), connection state is visual-only and `online` in production (only the DEV `?conn=` toggle changes it), the identity strip renders `—` because the router passes no pairing status, and `StuckShiftBadge` is never mounted. G0 therefore adds the two real duties: `ScreenTooSmall` below 1024px (the screen and its bridge calls never mount) and the persistent connection banner. The v5 wall forbids `/shell/` imports, so the connection store (plus its banner copy) and the viewport hook moved to neutral `renderer/connection/` and `renderer/viewport/`, re-exported from `shell/` (one store). TDD: the four parity tests fail against the old frame and pass with the change. Gates: full suite with coverage 519 files, 5,971 passed, 3 skipped; typecheck; changed-file ESLint/Prettier; diff check; renderer build with no v5 code in the bundle. **Open for the owner:** a sign-out control is missing from both shells — a capability gap, not a parity item, so it is not built here.
- **V5 sign-out (2026-09-26, `feat/023-v5-sign-out`, owner-approved):** adds the missing sign-out control to the V5 nav (`V5SignOut`). The first press asks inline (confirm / stay signed in; Escape cancels and focus returns). Confirm calls the existing `operator.signOut()` bridge method, and the renderer leaves `signedIn` only after main answers; a failure keeps the session and shows a generic, retryable error. The control is disabled, with a visible reason, while a payment attempt is `started`. No main, preload or bridge change. TDD: 7 of 8 tests RED first (the signed-out case passed first run). Full suite with coverage: 520 files, 5,979 passed, 3 skipped. **Recorded, not fixed:** main's `payments.discardOnSessionEnd` runs on any session end (including inactivity); whether it can discard an attempt holding live tender deserves the same check as #472.
